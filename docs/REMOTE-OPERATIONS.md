# Remote Operations Guide

## Environments

| Environment       | Quadlet Path                                         | App Port         | DB Volume                    | Image                                    |
| ----------------- | ---------------------------------------------------- | ---------------- | ---------------------------- | ---------------------------------------- |
| Remote demo       | `~/.config/containers/systemd/bridge-ph/lemans-demo` | `127.0.0.1:3002` | `lemans-remote-demo-db-data` | `lemans-bridge-dashboard:latest-alpine`  |
| Remote production | `~/.config/containers/systemd/bridge-ph/lemans`      | `127.0.0.1:3003` | `lemans-remote-prod-db-data` | `lemans-bridge-dashboard:lts-alpine`       |

## Caddy Integration

An existing rootless Caddy quadlet already handles public HTTP/HTTPS traffic. Each Le Mans environment deploys a **single lightweight bridge container** that joins both the internal app network and `caddy.network`:

- Remote demo bridge: `lemans-demo-caddy-bridge`
- Remote production bridge: `lemans-prod-caddy-bridge`

The app container is reachable by Caddy via its container name on the shared `caddy.network`. The app port (`127.0.0.1:3002/3003`) is only published for direct loopback health checks.

## Required Secrets

Create the environment file at the Quadlet path after deployment. The deployment script auto-generates database and Better Auth secrets, but Backblaze B2 credentials must be provided because they are API keys:

```bash
# Auto-generated by deploy-remote-demo.sh / deploy-remote-prod.sh
DATABASE_URL=postgresql://postgres:<generated>@lemans-remote-demo-db:5432/lemans_remote_demo_db?schema=public
BETTER_AUTH_SECRET=<auto-generated>
BETTER_AUTH_URL=http://127.0.0.1:3002

# Must be supplied by operator
B2_ENDPOINT=https://s3.us-west-004.backblazeb2.com
B2_REGION=us-west-004
B2_ACCESS_KEY_ID=<Backblaze key ID>
B2_SECRET_ACCESS_KEY=<Backblaze key secret>
B2_BUCKET_NAME=lemans-remote-demo-attachments
```

## Remote Demo Deployment

```bash
# From the project root on macOS
export REMOTE_HOST=vps.example.com
export REMOTE_USER=jk
export B2_ACCESS_KEY_ID=<your-b2-key-id>
export B2_SECRET_ACCESS_KEY=<your-b2-key-secret>
./scripts/deploy-remote-demo.sh
```

This will:
1. Build the app locally inside a `node:20-alpine` container.
2. Sync the standalone/static output to `/home/jk/bridge-ph/lemans-demo`.
3. Sync Quadlets to `~/.config/containers/systemd/bridge-ph/lemans-demo`.
4. Generate and install a remote `.env` file.
5. Start the DB and app systemd services.
6. Push schema and seed.
7. Enable a 30-minute reset timer and a manual reset service.

## Remote Production Deployment

```bash
export REMOTE_HOST=vps.example.com
export REMOTE_USER=jk
export B2_ACCESS_KEY_ID=<your-b2-key-id>
export B2_SECRET_ACCESS_KEY=<your-b2-key-secret>
./scripts/deploy-remote-prod.sh
```

Production follows the same flow but **does not seed the database** and enables a **daily B2 backup timer** instead of a reset timer.

## Manual Remote Demo Reset

```bash
ssh jk@vps.example.com
export PATH="/opt/podman/bin:$PATH"
/home/jk/bridge-ph/lemans-demo/reset-demo.sh
```

The reset script stops app + DB containers, recreates the DB volume, restarts services, re-runs `prisma db push` + `prisma db seed`, and deletes uploaded attachments from the demo B2 bucket.

## Backup Procedure (Production)

The production deployment installs `lemans-backup.service` and `lemans-backup.timer`, which runs daily:

```bash
# Runs automatically, or manually:
export PATH="/opt/podman/bin:$PATH"
/home/jk/bridge-ph/lemans/backup-prod.sh
```

The script dumps `lemans_remote_prod_db` with `pg_dump`, gzips it, and uploads it to `s3://lemans-remote-prod-attachments/backups/db/` using the B2 CLI.

## Restore Procedure

1. Stop the app container:
   ```bash
   systemctl --user stop lemans-remote-prod-app
   ```
2. Recreate the database volume or use a new volume.
3. Download the desired backup from Backblaze B2:
   ```bash
   b2 download-file-by-name lemans-remote-prod-attachments backups/db/lemans-prod-backup-YYYY-MM-DD-HHMMSS.sql.gz /tmp/restore.sql.gz
   gunzip /tmp/restore.sql.gz
   ```
4. Restore:
   ```bash
   podman exec -i lemans-remote-prod-db psql -U postgres -d lemans_remote_prod_db < /tmp/restore.sql
   ```
5. Restart the app and run health checks.

## Rollback Procedure

1. Re-tag the previous known-good digest on the registry:
   ```bash
   podman pull lemans-bridge-dashboard:lts-alpine-previous
   podman tag lemans-bridge-dashboard:lts-alpine-previous lemans-bridge-dashboard:lts-alpine
   ```
2. Restart the production container:
   ```bash
   systemctl --user restart lemans-remote-prod-app
   ```
3. Verify `http://127.0.0.1:3003/` returns `200 OK`.

## Health Checks

For remote environments, authenticate via `/api/auth/sign-in/email` with the deployed admin credentials, then check protected routes:

```bash
curl -I http://127.0.0.1:3002/login
curl -I http://127.0.0.1:3002/
```

## Notes

- Do not commit secrets to source control.
- Keep remote demo and production databases and buckets isolated.
- DNS and reverse proxy configuration are managed by the existing Caddy quadlet; only Caddy binds public ports.
- Use `node:20-alpine` and `postgres:16-alpine` for all images unless dependency compatibility explicitly requires a Debian-based image.
- All deployment containers and temporary build containers are `--rm` or explicitly removed.

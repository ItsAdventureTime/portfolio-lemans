# Next-Agent Handoff — Le Mans Demo Remediation

**From:** Lead Agent (OpenCode)
**To:** Next implementation/review/verification agent
**Date:** 2026-08-11
**Repository:** `/Users/jk.deguzman/dev/lemans-bridge-dashboard`
**Remote:** `https://github.com/ItsAdventureTime/bridge-lemans.git`
**Branch:** `main`
**Commit:** `71e7578`
**Status:** READY FOR REVIEW / REMOTE DEPLOY AUTHORIZATION

---

## 1. What this session did

Implemented R1–R9 from `reviews/NEXT-AGENT-REMEDIATION-REPORT-2026-08-11.md` and committed the result to `main`.

- Replaced basePath-unsafe Next.js Server Actions (`setRoleAction`, `enterDemoAction`) with basePath-safe API routes (`/api/set-role`, `/api/enter-demo`).
- Added `src/lib/api-url.ts` and `src/lib/base-path.ts` so client `fetch` URLs are correct under `/lemans/demo` and at root.
- Added `DemoSplash` simulated entry screen (`Enter as an Admin`) with no real authentication.
- Added `FormError` and `form-result.ts` and wired field-level/form-level error handling across customer, quotation, invoice, purchasing, and expense forms.
- Fixed select height, table link touch targets (`min-h-11 min-w-11`), and reduced-motion CSS.
- Added rootless Quadlet reset service/timer (`quadlet/remote-demo/lemans-demo-reset.container`, `.timer`) and deploy-script enable/start steps for 30-minute remote demo reset.
- Hardened `scripts/deploy-remote-demo.sh`: EXIT trap for temp files, mode-600 remote env, release manifest with image digests.
- Made `scripts/run-local.sh` generate a per-run DB password.
- Added Playwright E2E suite (`e2e/demo.spec.ts`, `playwright.config.ts`) with chromium, mobile, and reduced-motion projects.
- Updated `.gitignore` to exclude Playwright artifacts (`e2e-results.json`, `test-results/`).

---

## 2. State of play

### Completed and verified

- ✅ `./scripts/build.sh demo` and `./scripts/build.sh prod` succeed.
- ✅ `./scripts/verify-local.sh` passes (format/typecheck + Go generate/build/test).
- ✅ `./scripts/run-local.sh` starts DB + Go API + web on `127.0.0.1:3000/lemans/demo`.
- ✅ `./scripts/verify-vertical-slice.sh` returns 200 for all demo routes, DB port unpublished.
- ✅ Playwright E2E: 21/21 passed across `chromium`, `mobile`, `reduced-motion`.
- ✅ `./scripts/stop-local.sh` cleans up named local containers.

### Not yet done

- ⬜ Authorized remote demo deployment to the VPS (requires explicit user authorization + Caddy context).
- ⬜ Confirm the 30-minute reset timer actually fires on the remote host without touching production.
- ⬜ POST workflow E2E coverage beyond the customer-form validation spec.
- ⬜ Additional Go API unit tests for handlers and repository edge cases.

### Open decisions

1. Is the operator ready to authorize `REMOTE_HOST=... ./scripts/deploy-remote-demo.sh` now?
2. Should the next agent add more POST-workflow E2E specs (create quote → approve → convert → job order → invoice → collect) before remote deploy?

---

## 3. Files the next agent must read

1. `reviews/NEXT-AGENT-REMEDIATION-REPORT-2026-08-11.md` — original remediation brief.
2. `docs/DEMO-IMPLEMENTATION-PLAYBOOK.md` — authoritative demo behavior.
3. `docs/REMOTE-DEMO-DEPLOYMENT-PLAYBOOK.md` — remote deploy contract.
4. `docs/AGENT-EXECUTION-PROMPTS.md` — copy-paste prompts for verify/review/deploy.
5. `docs/MIGRATION-HANDOFF.md` — high-level handoff with full verification table.
6. `src/lib/api-url.ts` and `src/lib/base-path.ts` — basePath-safe URL strategy.
7. `e2e/demo.spec.ts` and `playwright.config.ts` — E2E coverage.

---

## 4. What NOT to do

- Do NOT add real authentication, login redirects, sessions, passwords, or `requireSession` checks to the demo profile.
- Do NOT run `podman system prune -a`, `podman rm -fa`, `podman machine rm/reset`, or any broad cleanup.
- Do NOT modify the remote VPS, Caddyfile, DNS, or shared Caddy container without explicit user authorization.
- Do NOT leave the local demo stack running after verification.
- Do NOT commit `e2e-results.json`, `test-results/`, or any `.env.*.tmp` files.

---

## 5. Copy-paste prompt for the next agent

```text
You are the next agent for the Le Mans demo build.

Start at commit 71e7578 on branch main in /Users/jk.deguzman/dev/lemans-bridge-dashboard.
Read these first:
  - reviews/NEXT-AGENT-REMEDIATION-REPORT-2026-08-11.md
  - docs/DEMO-IMPLEMENTATION-PLAYBOOK.md
  - docs/REMOTE-DEMO-DEPLOYMENT-PLAYBOOK.md
  - docs/AGENT-EXECUTION-PROMPTS.md
  - docs/MIGRATION-HANDOFF.md

Then run the local verification pipeline inside rootless Podman:
  export PATH="/opt/podman/bin:$PATH"
  podman machine start
  ./scripts/build.sh demo
  ./scripts/build.sh prod
  ./scripts/verify-local.sh
  ./scripts/run-local.sh
  ./scripts/verify-vertical-slice.sh
  npx playwright test
  ./scripts/stop-local.sh

Report every result with exact pass/fail counts. If any step fails, stop, read
the failing file, and fix the root cause before continuing. Do not claim the
pipeline passes if a single check failed or was skipped.

After local verification succeeds:
  1. If the user explicitly authorizes remote deployment, run the remote demo
     deploy prompt from docs/AGENT-EXECUTION-PROMPTS.md section 8 and verify the
     public URL, role switching, and reset timer status.
  2. If the user asks for more tests, extend e2e/demo.spec.ts with POST workflow
     coverage (create quote → approve → convert → job order → parts → supplier
     invoice → invoice → collection) without changing existing passing specs.
  3. Otherwise, update reviews/HANDOFF-2026-08-11.md with your exact results
     and stop cleanly.

Do not add real authentication, do not modify shared Caddy, and do not run
broad Podman cleanup commands.
```

---

## 6. Evidence links

- Commit: `71e7578` (`feat(demo): basePath-safe API routes, splash entry, form errors, reset timer, E2E`)
- Verification commands and results: see `docs/MIGRATION-HANDOFF.md` §5 and §6.
- E2E specs: `e2e/demo.spec.ts`
- New API routes: `src/app/api/set-role/route.ts`, `src/app/api/enter-demo/route.ts`
- Reset timer artifacts: `quadlet/remote-demo/lemans-demo-reset.container`, `quadlet/remote-demo/lemans-demo-reset.timer`

---

_End of handoff document._

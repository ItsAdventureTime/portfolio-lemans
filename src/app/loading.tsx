export default function Loading() {
  // Keep the shared layout mounted and interactive during section navigation.
  // Mutation-level busy states remain owned by their forms and actions.
  return null;
}

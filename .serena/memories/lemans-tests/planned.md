Need to add regression tests for: createCustomerAndVehicle, createSalesQuotation, createPurchaseRequest, createOpexRequest, supplier invoice allocation, DCS payment guards, dashboard live data, invoice detail page, middleware root protection. We can add lightweight tests using real db via integration if possible, but current tests are pure unit. We may add new test modules for auth/roles and for server actions using a test harness. However using db in tests requires DATABASE_URL. Could add integration tests that run against existing demo DB via prisma. Let's check tsconfig.test? None. Could add new test modules and invoke from index.ts. For server actions we'd need a session; we can test permission matrix via hasPermission. Add tests for: hasPermission('ROLE_DCS','disburseApprove') false, hasPermission('ROLE_DCS','disburseRecordPayment') true, canDisburseBePaid. Also test middleware protected paths and dashboard query functions. We can also add a HTTP smoke test in verify-vertical-slice script. The user requires regression tests for each fix. We will add unit tests where possible and update verify script with specific smoke checks.